{{- if and (.Values.postgresqlCluster.enabled) (.Values.postgresqlCluster.customAccessKeys.use) }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
  name: {{ .Values.postgresqlCluster.name }}-access-keys
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  ACCESS_KEY_ID: "{{ .Values.postgresqlCluster.customAccessKeys.accessKeyId }}"
  SECRET_ACCESS_KEY: "{{ .Values.postgresqlCluster.customAccessKeys.secretAccessKey }}"
{{- end }}
